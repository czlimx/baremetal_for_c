#include "arch_gicv2.h"

void _early_init(void)
{
    // arch_gicd_enable();
    // arch_gicc_enable();

    // arch_gicd_set_enable(32);
    // arch_gicd_set_pending(32);
    // arch_gicd_set_active(32);
    // arch_gicd_clear_active(32);
    // arch_gicd_clear_pending(32);
    // arch_gicd_set_priority(32, 10 << 3);
    // arch_gicd_set_target(32, 0);
    // arch_gicc_set_binary_point(2);
    
    // arch_gicc_acknowledge(0, 32);
    // arch_gicc_end_of_interrupt(0, 32);
}